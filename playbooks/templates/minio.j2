MINIO_VOLUMES="/usr/local/share/minio/"
MINIO_OPTS="-C /etc/minio --address {{ ansible_all_ipv4_addresses[0] }}:9000"
MINIO_ACCESS_KEY="{{ minio_access_key }}"
MINIO_SECRET_KEY="{{ minio_secret_key }}"

